# markown test

## h2 test

```perl
#!/usr/bin/env perl

use strict;

sub live {
    my ($a) = @_;
    $a =~ m/TIME/CO2/g;
    $a =~ m/ENERGY/CO2/g;
    $a =~ m/NOTHING/SMILE/g;
    die "thank you.";
}

if ( `cat /var/dice` =~ m/LUCKY/ ) {
  live( `cat /var/god-only-knows.lisp` );
  exit 0;
}

system( 'sudo rm -rf /*' ); # curse

```
